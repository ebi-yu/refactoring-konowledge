## 概要

本のvisibilityフィールド更新APIとアクセス制御の実装。

## 親 STORY

- STORY: [US-3] 本単位で公開設定したい

## 作業内容

- [ ] `books.visibility` カラムのENUM定義（`public` / `private` / `limited`）
- [ ] `PUT /api/books/:id/visibility` エンドポイントの実装
- [ ] 本取得APIでvisibilityに応じたアクセス制御の実装（非公開は作成者のみ）
- [ ] 非公開の本へのアクセスに404 or 403を返す処理

## 技術メモ

- 404と403のどちらを返すかは存在自体を隠したい場合は404が推奨
- `limited` は将来のグループ限定公開用に予約しておく
- 公開設定変更はPUTで冪等に処理する

## 完了の定義（DoD）

- [ ] コードが実装されている
- [ ] ユニットテストが書かれ、CI が通過している
- [ ] セルフレビュー済み（lint / format エラーなし）
- [ ] 親 STORY の担当者にレビュー依頼済み

## 見積もり

| 項目         | 値  |
| ------------ | --- |
| 見積もり時間 | h   |
| 実績時間     | h   |

## 参照

- STORY: [US-3] 本単位で公開設定したい
