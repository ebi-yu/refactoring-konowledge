## 概要

POST /api/admin/users/:id/block エンドポイントの実装（管理者権限チェック含む）。

## 親 STORY

- STORY: [US-2] 問題のあるユーザーをブロックしたい

## 作業内容

- [ ] `POST /api/admin/users/:id/block` エンドポイントの実装（ステータスをblockedに変更）
- [ ] `POST /api/admin/users/:id/unblock` エンドポイントの実装（ブロック解除）
- [ ] 管理者ロールチェックミドルウェアの適用
- [ ] 自分自身をブロックできないガードの実装
- [ ] ブロック操作の監査ログ記録

## 技術メモ

- `users.status` カラムに `active` / `blocked` のENUM値を使用
- ブロック操作は管理者自身には適用できないバリデーションを追加
- 監査ログは `admin_audit_logs` テーブルに記録する

## 完了の定義（DoD）

- [ ] コードが実装されている
- [ ] ユニットテストが書かれ、CI が通過している
- [ ] セルフレビュー済み（lint / format エラーなし）
- [ ] 親 STORY の担当者にレビュー依頼済み

## 見積もり

| 項目         | 値  |
| ------------ | --- |
| 見積もり時間 | h   |
| 実績時間     | h   |

## 参照

- STORY: [US-2] 問題のあるユーザーをブロックしたい
