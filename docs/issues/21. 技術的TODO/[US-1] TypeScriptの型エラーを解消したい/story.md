## 概要

TypeScriptの型エラーをすべて解消し、型安全なコードベースを実現する。

## 親 EPIC

- EPIC: (21) 技術的TODO

## ユーザーストーリー

**開発者** として、**TypeScriptの型エラーを解消** したい。なぜなら **型安全性を確保してバグを未然に防ぎ、コードの品質を高めたい** からだ。

## 作業内容

- [ ] TASK 1: tsgo --noEmitで型チェックを通す（別ファイルで詳細管理）

## 受け入れ基準

> Given / When / Then 形式で記述する。

- [ ] **Given** TypeScriptの型エラーが存在する状態で
      **When** `tsgo --noEmit` を実行したとき
      **Then** 型エラーが0件になり、コマンドが正常終了すること

## 完了の定義（DoD）

- [ ] 配下のすべてのTaskが完了している
- [ ] ビルドが通る
- [ ] セルフレビュー済み（lint / format エラーなし）
