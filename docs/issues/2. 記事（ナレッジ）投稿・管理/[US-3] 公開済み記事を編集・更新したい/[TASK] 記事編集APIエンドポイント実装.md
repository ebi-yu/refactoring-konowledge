## 概要

PUT/PATCH /api/articles/:id エンドポイントの実装（自分の記事のみ更新可能な権限チェック含む）。

## 親 STORY

- STORY: [US-3] 公開済み記事を編集・更新したい

## 作業内容

- [ ] `PUT /api/articles/:id` エンドポイントを実装する
- [ ] リクエストボディで `title`・`body`・`visibility` を受け取り記事を更新する
- [ ] `article.author_id !== session.userId` の場合は 403 を返す
- [ ] 存在しない記事 ID の場合は 404 を返す
- [ ] `updated_at` を現在時刻で更新する

## 技術メモ

- 管理者ユーザーは他者の記事も更新できるよう権限チェックを拡張可能にしておく
- 楽観的ロック（`updated_at` の一致チェック）を将来追加できる設計にする

## 完了の定義（DoD）

- [ ] コードが実装されている
- [ ] ユニットテストが書かれ、CI が通過している
- [ ] セルフレビュー済み（lint / format エラーなし）
- [ ] 親 STORY の担当者にレビュー依頼済み

## 見積もり

| 項目         | 値  |
| ------------ | --- |
| 見積もり時間 | h   |
| 実績時間     | h   |

## 参照

- STORY: [US-3] 公開済み記事を編集・更新したい
