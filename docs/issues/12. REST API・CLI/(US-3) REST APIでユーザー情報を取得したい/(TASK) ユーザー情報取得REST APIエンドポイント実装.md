## 概要

GET /api/v1/users および GET /api/v1/users/:id エンドポイントの実装（管理者権限チェック）。

## 親 Story

- Story: (US-3) REST APIでユーザー情報を取得したい

## 作業内容

- [ ] `GET /api/v1/users` エンドポイントの実装（ユーザー一覧・ページネーション・管理者トークン必須）
- [ ] `GET /api/v1/users/:id` エンドポイントの実装（個別ユーザー情報取得）
- [ ] 管理者APIトークン専用の権限チェックミドルウェアの実装
- [ ] レスポンスフィールドの選択（機密情報の除外）

## 技術メモ

- レスポンスにはid・name・email・role・created_atのみ含め、パスワードハッシュ等は除外する
- 管理者トークンの判定はトークンに紐づくユーザーのroleで行う
- ページネーションのデフォルトlimitは20件とする

## 完了の定義（DoD）

- [ ] コードが実装されている
- [ ] ユニットテストが書かれ、CI が通過している
- [ ] セルフレビュー済み（lint / format エラーなし）
- [ ] 親 Story の担当者にレビュー依頼済み

## 見積もり

| 項目         | 値  |
| ------------ | --- |
| 見積もり時間 | h   |
| 実績時間     | h   |

## 参照

- Story: (US-3) REST APIでユーザー情報を取得したい
