## 概要

GET /api/health エンドポイントの実装（DB・ストレージ接続確認含む）。

## 親 STORY

- STORY: (US-4) ヘルスチェックエンドポイントを確認したい

## 作業内容

- [ ] `GET /api/health` エンドポイントの実装
- [ ] DBとストレージのヘルスチェックサービスの呼び出し
- [ ] 全て正常な場合に `{ status: "ok", checks: {...} }` と200を返す処理
- [ ] いずれかが異常な場合に503を返す処理
- [ ] 認証不要でアクセスできる公開エンドポイントとして設定

## 技術メモ

- レスポンス例: `{ "status": "ok", "checks": { "db": "ok", "storage": "ok" } }`
- 異常時: `{ "status": "error", "checks": { "db": "error", "storage": "ok" } }` + HTTP 503
- ヘルスチェック自体のタイムアウトは3秒に設定して素早く応答する

## 完了の定義（DoD）

- [ ] コードが実装されている
- [ ] ユニットテストが書かれ、CI が通過している
- [ ] セルフレビュー済み（lint / format エラーなし）
- [ ] 親 STORY の担当者にレビュー依頼済み

## 見積もり

| 項目         | 値  |
| ------------ | --- |
| 見積もり時間 | h   |
| 実績時間     | h   |

## 参照

- STORY: (US-4) ヘルスチェックエンドポイントを確認したい
