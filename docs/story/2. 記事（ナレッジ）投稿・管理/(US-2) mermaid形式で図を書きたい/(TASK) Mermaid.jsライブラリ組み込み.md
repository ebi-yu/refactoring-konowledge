## 概要

mermaid.js をプロジェクトに追加し、初期化設定を行う。

## 親 Story

- Story: (US-2) mermaid形式で図を書きたい

## 作業内容

- [ ] `mermaid` パッケージを依存関係に追加する（`npm install mermaid`）
- [ ] アプリケーションエントリーポイントで Mermaid.js を初期化する
- [ ] テーマ設定（ライト / ダークモード対応）を行う
- [ ] セキュリティ設定（`securityLevel` の適切な設定）を確認する

## 技術メモ

- Mermaid.js はクライアントサイドでのみ動作するため、SSR 環境では動的インポート（`import('mermaid')`）を使用する
- `startOnLoad: false` に設定してプログラマティックに描画を制御する

## 完了の定義（DoD）

- [ ] コードが実装されている
- [ ] ユニットテストが書かれ、CI が通過している
- [ ] セルフレビュー済み（lint / format エラーなし）
- [ ] 親 Story の担当者にレビュー依頼済み

## 見積もり

| 項目 | 値 |
|------|----|
| 見積もり時間 | h |
| 実績時間 | h |

## 参照

- Story: (US-2) mermaid形式で図を書きたい
