## ユーザーストーリー

| 項目 | 内容 |
|------|------|
| **Who**（誰が） | 管理者 |
| **What**（何をしたい） | ファイルの保存先をS3 / R2 / MinIO等のクラウドストレージに切り替えたい |
| **Why**（なぜ・価値） | DBにファイルを保存すると容量と性能の問題が出るから |

**ストーリー文（上記を文章化）:**

> **管理者** として、**ファイルの保存先をS3 / R2 / MinIO等のクラウドストレージに切り替えたい**。なぜなら **DBにファイルを保存すると容量と性能の問題が出る** からだ。

## 背景・補足

環境変数でS3/R2/MinIOを切り替えられるストレージアダプター層を設ける。本番はS3/R2、ローカル開発はMinIOを使う構成を想定する。

## 受け入れ基準

- [ ] **Given** .envにS3接続情報が設定されている  
      **When** ファイルをアップロードする  
      **Then** S3バケットに保存される

- [ ] **Given** MinIO設定  
      **When** アップロードする  
      **Then** MinIOに保存される

## スコープ・制約

| 項目 | 内容 |
|------|------|
| **優先度** | `Must` |
| **旧システム対応** | 🆕 新規 |
| **スコープ** | `[x]` 実装 |

## タスク一覧

- [ ] (TASK) S3・R2・MinIO接続ライブラリ設定
- [ ] (TASK) ストレージ切り替え設定実装

## 参照

- Epic: 15. インフラ・運用/epic.md
- 関連: Epic 11 US-4（管理画面から同設定を動的に変更する機能。このインフラ実装が前提となる）
