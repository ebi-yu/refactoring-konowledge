## ユーザーストーリー

| 項目 | 内容 |
|------|------|
| **Who**（誰が） | 管理者 |
| **What**（何をしたい） | DBとファイルストレージのバックアップ・リストア手順とスクリプトを使いたい |
| **Why**（なぜ・価値） | 障害時に素早くデータを復元できるようにしておきたいから |

**ストーリー文（上記を文章化）:**

> **管理者** として、**DBとファイルストレージのバックアップ・リストア手順とスクリプトを使いたい**。なぜなら **障害時に素早くデータを復元できるようにしておきたい** からだ。

## 背景・補足

pg_dumpによるPostgreSQLのバックアップスクリプトと、クラウドストレージのファイルをローカルに同期するスクリプトを`scripts/`ディレクトリに配置する。

## 受け入れ基準

- [ ] **Given** バックアップスクリプト  
      **When** `./scripts/backup.sh`を実行する  
      **Then** DBダンプとファイル一覧が指定のパスに保存される

- [ ] **Given** バックアップファイル  
      **When** リストアスクリプトを実行する  
      **Then** データが復元される

## スコープ・制約

| 項目 | 内容 |
|------|------|
| **優先度** | `Should` |
| **旧システム対応** | 🆕 新規 |
| **スコープ** | `[x]` 実装 |

## タスク一覧

- [ ] (TASK) DBバックアップスクリプト作成
- [ ] (TASK) ファイルストレージバックアップスクリプト作成

## 参照

- Epic: 15. インフラ・運用/epic.md
