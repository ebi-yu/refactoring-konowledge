## ユーザーストーリー

| 項目 | 内容 |
|------|------|
| **Who**（誰が） | 管理者 |
| **What**（何をしたい） | `/api/health`にアクセスしてサービスの死活を確認したい |
| **Why**（なぜ・価値） | 監視ツールやKubernetesからヘルスチェックできる口が必要だから |

**ストーリー文（上記を文章化）:**

> **管理者** として、**`/api/health`にアクセスしてサービスの死活を確認したい**。なぜなら **監視ツールやKubernetesからヘルスチェックできる口が必要だから** だ。

## 背景・補足

GET /api/health でDBとストレージの疎通チェックを行い、全て正常なら200・status:okを返す。DBに接続できない場合は503を返して外部監視ツールにアラートを上げる。

## 受け入れ基準

- [ ] **Given** 全サービスが正常  
      **When** GET /api/health にアクセスする  
      **Then** 200とstatus:okが返る

- [ ] **Given** DBに接続できない  
      **When** GET /api/health にアクセスする  
      **Then** 503が返る

## スコープ・制約

| 項目 | 内容 |
|------|------|
| **優先度** | `Must` |
| **旧システム対応** | 🆕 新規 |
| **スコープ** | `[x]` 実装 |

## タスク一覧

- [ ] (TASK) ヘルスチェックAPIエンドポイント実装
- [ ] (TASK) DB・ストレージ死活監視実装

## 参照

- Epic: 15. インフラ・運用/epic.md
