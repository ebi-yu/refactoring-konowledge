## 概要

PostgreSQL tsvector インデックスまたは外部検索エンジンのインデックス設定。

## 親 Story

- Story: (US-1) キーワードで全文検索したい

## 作業内容

- [ ] PostgreSQL を使う場合：`articles` テーブルに `search_vector tsvector` カラムを追加し GIN インデックスを作成する
- [ ] `search_vector` を `title` + `body` から自動更新するトリガーを実装する
- [ ] 日本語検索に対応するため `pg_bigm` 拡張または形態素解析（PGroonga 等）を設定する
- [ ] 外部検索エンジンを使う場合：インデックス設定（フィールド・スコアリング・日本語トークナイザー）を構成する

## 技術メモ

- `pg_bigm` は2グラムインデックスで日本語検索に適する
- Meilisearch は設定が簡単で日本語にも対応しているが、別サービスのデプロイが必要

## 完了の定義（DoD）

- [ ] コードが実装されている
- [ ] ユニットテストが書かれ、CI が通過している
- [ ] セルフレビュー済み（lint / format エラーなし）
- [ ] 親 Story の担当者にレビュー依頼済み

## 見積もり

| 項目 | 値 |
|------|----|
| 見積もり時間 | h |
| 実績時間 | h |

## 参照

- Story: (US-1) キーワードで全文検索したい
