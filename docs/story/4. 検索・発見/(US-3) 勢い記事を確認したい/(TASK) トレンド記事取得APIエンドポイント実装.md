## 概要

GET /api/articles/trending エンドポイントの実装（24h/7d 期間指定対応）。

## 親 Story

- Story: (US-3) 勢い記事を確認したい

## 作業内容

- [ ] `GET /api/articles/trending?period=24h` エンドポイントを実装する
- [ ] `period` パラメータで `24h`・`7d` を切り替えて対応する期間のスコアを返す
- [ ] トレンドスコア降順で上位 N 件（例: 20件）を返す
- [ ] ログイン状態に応じた visibility フィルタリングを適用する
- [ ] レスポンスに記事情報・スコア・閲覧数を含める

## 技術メモ

- `article_trending_scores` テーブルから期間別スコアを取得する
- キャッシュ（Redis TTL: 5分）を設定してパフォーマンスを確保する

## 完了の定義（DoD）

- [ ] コードが実装されている
- [ ] ユニットテストが書かれ、CI が通過している
- [ ] セルフレビュー済み（lint / format エラーなし）
- [ ] 親 Story の担当者にレビュー依頼済み

## 見積もり

| 項目 | 値 |
|------|----|
| 見積もり時間 | h |
| 実績時間 | h |

## 参照

- Story: (US-3) 勢い記事を確認したい
